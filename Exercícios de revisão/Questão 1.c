#include <stdlib.h>
#include <stdio.h>
/*1 - Faça um programa que calcule a soma dos números de 1 a 100.*/

int main(){
int num, soma = 1, i, result;
for (i = 1; i <= 100; i++){
    result = num + soma;
    printf("%d + %d = %d\n", num, num, result);
    }
}
